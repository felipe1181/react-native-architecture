import {PropsRoute} from 'core/routes/interfaces';
import {HomeScreen} from './pages';

const Routes: Array<PropsRoute> = [
  {
    name: 'Home',
    Component: HomeScreen,
  },
];

export default Routes;
